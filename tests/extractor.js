
var chai = require("chai");
var expect = chai.expect;

var Extractor = require("../src/extractor");

var ext = new Extractor();

describe("Extractor", () => {
    
    it("Extracts path info from an URL", () => {
        
        
    });
    
    
    it("Rejects incorrectly formed URLs", () => {
        
    });
    
});